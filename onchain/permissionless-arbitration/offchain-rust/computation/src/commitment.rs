pub mod commitment {
    use utils::bint::bint::Bint;
    use crate::interval;
    fn build_small_machine_commitment() {}

    fn build_big_machine_commitment() {}

    fn build_commitment() {}

    const bint: Bint = Bint {
        bits: 256,
        data: vec![],
    };
}
